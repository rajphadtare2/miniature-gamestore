package minigameacle.order.enums;

public enum OrderStatus {
    PENDING,   // Order is placed but not finalized
    COMPLETED, // Order is successfully placed
    FAILED     // Order placement failed
}
